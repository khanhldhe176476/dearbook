spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000

  jpa:
    hibernate:
      ddl-auto: validate

      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

server:
  port: 8080
  error:
    include-stacktrace: never
  tomcat:
    max-swallow-size: -1
    max-http-form-post-size: -1

app:
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:Mh123#@!}
    jwt:
      secret: ${ADMIN_JWT_SECRET:dearbook-admin-secret-key-32chars!!}
  jwt:
    secret: ${JWT_SECRET:your-256-bit-secret-your-256-bit-secret-for-local-dev-only}
    expiration-ms: 86400000

google:
  client-id: ${GOOGLE_CLIENT_ID:your-google-client-id.apps.googleusercontent.com}
  aws:
    s3:
      bucket-name: ${AWS_S3_BUCKET_NAME:dearbook-assets}
      region: ${AWS_REGION:ap-southeast-1}
    credentials:
      access-key: ${AWS_ACCESS_KEY_ID:your-access-key}
      secret-key: ${AWS_SECRET_ACCESS_KEY:your-secret-key}

